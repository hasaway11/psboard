<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.psboard.member.dao.MemberDao">
	<insert id="save">
		insert into member(username, password, email, role, levels, profile)
		values(#{username}, #{password}, #{email}, #{role}, #{level}, #{profile})
	</insert>
</mapper>